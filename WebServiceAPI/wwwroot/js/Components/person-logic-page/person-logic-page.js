define(["knockout"], function(ko) {
    return function(params) {
        let leftArrow = ko.observable("left-arrow");
        
        //Aktuelle people > 2010 as cards
        
        //People from different eras as a list with three examples each list
        
        
        return {
            leftArrow: leftArrow
        }
    }
})